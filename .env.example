# Where the Vite dev proxy forwards /api requests. Defaults to localhost:8000.
# The API key itself is NOT stored here — it is entered in the app's Settings
# screen and kept in the browser's localStorage so it never lands in the repo.
FINLEDGER_API_URL=http://localhost:8000
